provider:
  name: tastytrade
  client_secret: "${TASTYTRADE_CLIENT_SECRET}"
  refresh_token: "${TASTYTRADE_REFRESH_TOKEN}"
  is_paper: true

engine:
  risk_free_rate: 0.05
  dividend_yield: 0.0

visualization:
  theme: bloomberg
